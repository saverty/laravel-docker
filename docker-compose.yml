version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laravel_app
    volumes:
      - "./:/laravel-docker"

  #Nginx Service
  webserver:
    image: nginx:alpine
    container_name: laravel_webserver
    restart: unless-stopped
    working_dir: /laravel-docker
    tty: true
    ports:
      - "8005:8005"
    volumes:
      - "./:/laravel-docker"
      - "./server.conf:/etc/nginx/conf.d/default.conf:ro"
    links:
      - app
